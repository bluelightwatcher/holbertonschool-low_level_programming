#include <stdio.h>
/**
 * Main-entry point
 * printf-will used to display a string
 * return: 0
 */
int main(void)
{
	puts("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

#include "main.h"

/**
 * _strlen - loops over the string as an array
 * @s: is a character
 * Return: final value of i *
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);

}
